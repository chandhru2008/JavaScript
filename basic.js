//Variables in java script
//Variables store values and allow us to reference them later in the code. You can declare variables using


// let: Declares a variable that can be reassigned.
let name = "Chandhru";

// const: Declares a constant variable (its value can't be reassigned).
const age = 18;

// var: Older way of declaring variables (less common now).
var mark = 80.34;

//Print details
console.log(name);
console.log(age);
console.log(mark);

//Data types in java scripts 

// String: Text (e.g., "Hello")
let String = "This is a String";

// Number: Numeric values (e.g., 10, 3.14)
let Number1 = 10
let Number2 = 3.14;

// Boolean: True or False (e.g., true, false)
let boolean = false //Or ture 

// Object: Collections of key-value pairs (e.g., { name: "John", age: 30 })
let object = {name:"John",age:30};

// Array: Ordered collections of values (e.g., [1, 2, 3])
let array = [1,2,3];

// Null: A special value representing no value or object
console.log(null);

// Undefined: A variable that has been declared but not assigned a value
console.log(a) // a not definded in the programe so this will print undefinded

